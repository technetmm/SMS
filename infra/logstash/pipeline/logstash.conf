input {
  tcp {
    port => 5000
    codec => json
  }
}

filter {
  mutate {
    add_field => { "service" => "lms" }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "lms-logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
